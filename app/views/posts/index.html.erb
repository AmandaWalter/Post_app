<h1>Posts</h1>
<p><%= link_to 'New post',new_post_path %></p>
<% @posts.each do |post| %>
	<h2><%= post.title %></h2>
	<h2><%= post.author %></h2>
	<p><%= post.body %></p>
	<%= link_to 'Show post',post_path(post.id) %>
	<% end %>
